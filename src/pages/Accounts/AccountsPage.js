// src/pages/Accounts/AccountsPage.js

import AccountService from '../../services/accountService.js';

const AccountsPage = {
  render: async (container) => {
    // TODO: obtener cuentas con AccountService.getAll()
    // TODO: renderizar lista de cuentas
    // TODO: agregar botones de crear / editar / eliminar
  },
};

export default AccountsPage;
